The SQL-to-engine flow code has already been implemented in this codebase. I do not want to rewrite it.

Please inspect the existing code and help me verify the completed implementation for Part 1.

Context:
Srajan generates a SQL query. The existing code should take raw input/feed files, create Spark DataFrames/temp views, execute the SQL, generate a transformed output file, and make that file ready for the reconciliation engine.

I need you to trace the existing implementation and explain exactly how it works.

Please identify the exact classes, methods, and config properties involved in this flow:

1. Where input/feed files are read from config
2. Where input files are converted into Spark DataFrames
3. Where temp views are created
4. How temp view names are derived from file names
5. Where lookup files are loaded
6. Where lookup temp views are created
7. Where the SQL query is read from
8. Where spark.sql or equivalent SQL execution happens
9. Where the transformed DataFrame is created
10. Where the transformed output file is written
11. Where the output file is handed off to the engine or made available for engine consumption

Please search for these keywords:
- spark.sql
- createOrReplaceTempView
- createTempView
- Dataset<Row>
- DataFrame
- loadLookup
- loadLookups
- lookup
- application-local.properties
- sql
- query
- transformation
- output
- write

I need a clear mapping like this:

| Step | Existing class/method | What it does | Config/property used |
|---|---|---|---|
| Load input files |  |  |  |
| Create temp views |  |  |  |
| Load lookup files |  |  |  |
| Read SQL |  |  |  |
| Execute SQL |  |  |  |
| Write output |  |  |  |

Also check if the implementation supports a baseline test SQL like:

SELECT * FROM left_feed

Please tell me:
1. What local files/folders I need
2. What config values I need in application-local.properties
3. What command or main class I should run
4. What logs/output I should expect
5. What could fail if view names do not match SQL table names
6. Whether any part is hard-coded
7. Whether the implementation is ready for Srajan’s generated SQL

Do not generate new code unless something is missing. First explain the existing flow and only recommend fixes if needed.
